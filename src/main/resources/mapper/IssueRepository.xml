<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.regyl.gfi.repository.IssueRepository">

    <insert id="saveAll">
        INSERT INTO gfi.${tableName} (source_id, title, url, updated_at, created_at, repository_id)
        VALUES
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.sourceId}, #{entity.title}, #{entity.url}, #{entity.updatedAt}, #{entity.createdAt}, #{entity.repositoryId})
        </foreach>
        ON CONFLICT (source_id) DO NOTHING
    </insert>

</mapper>
