<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.regyl.gfi.repository.RepoRepository">

    <resultMap id="RepositoryEntityResultMap" type="com.github.regyl.gfi.entity.RepositoryEntity">
        <id property="id" column="id"/>
        <result property="created" column="created"/>
        <result property="sourceId" column="source_id"/>
        <result property="title" column="title"/>
        <result property="url" column="url"/>
        <result property="stars" column="stars"/>
        <result property="description" column="description"/>
        <result property="language" column="language"/>
    </resultMap>

    <insert id="saveAll">
        INSERT INTO gfi.${tableName} (source_id, title, url, stars, description, language)
        VALUES
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.sourceId}, #{entity.title}, #{entity.url}, #{entity.stars}, #{entity.description}, #{entity.language})
        </foreach>
        ON CONFLICT (source_id) DO NOTHING
    </insert>

    <select id="findAll" resultMap="RepositoryEntityResultMap">
        SELECT
            id,
            created,
            source_id,
            title,
            url,
            stars,
            description,
            language
        FROM gfi.${tableName}
    </select>

</mapper>
